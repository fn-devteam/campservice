import PropTypes from 'prop-types';
declare type IconProps = {
    iconName: string;
    color?: string;
};
declare const Icon: {
    ({ iconName, color }: IconProps): any;
    defaultProps: {
        color: string;
    };
    propTypes: {
        color: PropTypes.Requireable<string>;
    };
};
export default Icon;
